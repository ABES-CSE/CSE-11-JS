//Anonymous and change the way this binds in functions.

let mult=(x,y)=>{
    console.log(x*y);
}

let a=10,b=11;
let add=(a,b)=>{
    console.log(a+b);
}
add(a,b);

//Object: Key-value pair.

let x=new Object();
let obj={};

let person={
    name: John,
    age: 30,
    address:{
        street: Element
    }
};
console.log(person.name);

//Array: Ordered collection of data either primitive or object. Array is an special type of Object.
let myInfo=new Array();

let collection={

    {},[],true,"john",function(){},9870754,undefined,null,new String("abc"),new Date()
};

let trainingInfo=["Jitendra",29];

